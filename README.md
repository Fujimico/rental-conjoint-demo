# rental-conjoint-pages-demo

Vite + React の静的サイトとして、Cloudflare Pages にそのままデプロイできるデモです。

## ローカル起動
```bash
npm install
npm run dev
```

## ビルド
```bash
npm run build
npm run preview
```

Cloudflare Pages では Build command: `npm run build` / Output directory: `dist`
